# IaC for Open Telekom Cloud

To get started, you need to generate the pulumi bindings for the Open Telekom Cloud terraform provider:

```bash
> pulumi install
> uv sync
```

Afterwards, you can run `pulumi up` to deploy the infrastructure.
